apply plugin: 'java'
apply plugin: 'eclipse'
group = "com.ramiz.testone"
version = '1.0'
sourceCompatibility = 1.7
targetCompatibility = 1.7

//create a single Jar with all dependencies
task fatJar(type: Jar) {
	manifest {
        attributes 'Implementation-Title': 'Gradle Jar File Example',  
        	'Implementation-Version': version,
        	'Main-Class': 'mapreduce.Main'
    }
    baseName = project.name + '-all'
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}

jar {
    manifest {
        attributes 'Implementation-Title': 'Gradle Jar File Example',
                   'Implementation-Version': version,
                   'Main-Class': 'logic.Main'
    }
}

//Get dependencies from Maven central repository
repositories {
    mavenCentral()
    jcenter()
    maven {
        url "https://repository.cloudera.com/artifactory/cloudera-repos/"
    }
}

//Project dependencies
dependencies {
	compile 'ch.qos.logback:logback-classic:1.1.2'
    compile 'org.apache.hadoop:hadoop-client:2.0.0-mr1-cdh4.0.1'
    compile 'com.twitter:parquet-hadoop:1.0.0'
	compile 'junit:junit:4.8.2'
    compile 'com.twitter:parquet-tools:1.4.3'
    compile 'org.projectlombok:lombok:1.16.6'
    compile 'org.apache.hbase:hbase:0.94.3'
    compile group: 'org.slf4j', name: 'slf4j-api', version: '1.7.+'
    compile group: 'ch.qos.logback', name: 'logback-classic', version: '1.+'
    //classpath 'eu.appsatori:gradle-fatjar-plugin:0.3'
}


eclipse {
     //Ensure Eclipse build output appears in build directory
    classpath {
        defaultOutputDir = file("${buildDir}/eclipse-classes")
    }
<<<<<<< HEAD
}
=======
}
>>>>>>> b3be5d4... modified input value for hbase cell. Now, it's timestamp. Added support for multiple segments per one input line (list is used)
